Things to do to do a release:


- update the $VERSION at the top of src/termdetect

- do all this:
    git checkout release
    git merge master
    cd src; make
    cp termdetect.fatpacked ../termdetect.release
    git add ../termdetect.release
    git commit 
    git push origin release

- check that the new version actually shows up at http://is.gd/termdet




http://is.gd/stats.php?url=termdet
